{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "$id": "https://signalcraft.dev/schemas/replay_safe_artifact.schema.v1.json",
  "title": "Replay-safe artifact schema v1",
  "description": "Contract for artifacts required for replay verification. May contain full job data, config, hashes. Used for deterministic replay and drift detection. See docs/ARTIFACT_MODEL.md.",
  "type": "object",
  "required": ["replay_safe_artifact_schema_version"],
  "properties": {
    "replay_safe_artifact_schema_version": {
      "type": "integer",
      "enum": [1],
      "description": "Schema version for replay-safe artifact contract."
    },
    "artifact_type": {
      "type": "string",
      "enum": ["replay_safe"],
      "description": "Identifies this as a replay-safe artifact."
    },
    "run_id": {
      "type": "string",
      "description": "Run identifier for replay correlation."
    },
    "hashes": {
      "type": "object",
      "description": "Content hashes for replay verification."
    }
  },
  "additionalProperties": true
}
