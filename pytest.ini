[pytest]
markers =
    aws_integration: requires live AWS credentials/network access; skipped unless --run-aws-integration is provided
    slow: heavier end-to-end tests that can be excluded from fast feedback loops
